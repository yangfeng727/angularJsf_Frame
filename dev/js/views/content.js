;define(['app','service','xhrConfig'],
    function (app,service,xhrConfig) {
        // 使用严格模式
        'use strict';

        return function(){
            app.register.controller('ctr_content', function ($scope,$http,testService) {

            });
        }
    });